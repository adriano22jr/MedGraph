export declare const primitiveUv1Padding: number;
export declare const primitiveUv1PaddingScale: number;
export declare function createConeData(baseRadius: number, peakRadius: number, height: number, heightSegments: number, capSegments: number, roundedCaps: boolean): {
    positions: number[];
    normals: number[];
    uvs: number[];
    uvs1: number[];
    indices: number[];
};
