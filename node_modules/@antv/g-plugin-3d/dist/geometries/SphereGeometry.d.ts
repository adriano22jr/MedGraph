import type { Device } from '@antv/g-device-api';
import { ProceduralGeometry } from './ProceduralGeometry';
export interface SphereGeometryProps {
    radius: number;
    latitudeBands: number;
    longitudeBands: number;
}
export declare class SphereGeometry extends ProceduralGeometry<SphereGeometryProps> {
    constructor(device: Device, props?: Partial<SphereGeometryProps>);
    get radius(): number;
    set radius(v: number);
    get latitudeBands(): number;
    set latitudeBands(v: number);
    get longitudeBands(): number;
    set longitudeBands(v: number);
    createTopology(): {
        indices: number[];
        positions: number[];
        normals: number[];
        uvs: number[];
        uv1s: number[];
    };
}
