import type { Device } from '@antv/g-device-api';
import { ProceduralGeometry } from './ProceduralGeometry';
export interface PlaneGeometryProps {
    width: number;
    depth: number;
    widthSegments: number;
    depthSegments: number;
}
export declare class PlaneGeometry extends ProceduralGeometry<PlaneGeometryProps> {
    get width(): number;
    set width(v: number);
    get depth(): number;
    set depth(v: number);
    get widthSegments(): number;
    set widthSegments(v: number);
    get depthSegments(): number;
    set depthSegments(v: number);
    constructor(device: Device, props?: Partial<PlaneGeometryProps>);
    createTopology(): {
        indices: number[];
        positions: number[];
        normals: number[];
        uvs: number[];
        uv1s: number[];
    };
}
