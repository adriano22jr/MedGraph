import { RenderInst } from './RenderInst';
export declare class RenderInstPool {
    pool: RenderInst[];
    allocCount: number;
    allocRenderInstIndex(): number;
    popRenderInst(): void;
    reset(): void;
    destroy(): void;
}
