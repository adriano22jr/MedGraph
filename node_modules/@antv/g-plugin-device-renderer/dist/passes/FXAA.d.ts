import type { RGGraphBuilder } from '../render/interfaces';
import type { RenderHelper } from '../render/RenderHelper';
interface RenderInput {
    backbufferWidth: number;
    backbufferHeight: number;
}
export declare function pushFXAAPass(builder: RGGraphBuilder, renderHelper: RenderHelper, renderInput: RenderInput, mainColorTargetID: number): void;
export {};
