import type { DisplayObject } from '@antv/g-lite';
export declare class PickingIdGenerator {
    private counter;
    private id2DisplayObjectMap;
    getId(displayObject: DisplayObject): number;
    getById(id: number): DisplayObject;
    deleteById(id: number): void;
    reset(): void;
    decodePickingColor(color: Uint8Array): number;
    encodePickingColor(featureIdx: number): [number, number, number];
}
