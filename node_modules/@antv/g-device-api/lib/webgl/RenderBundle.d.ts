import { RenderBundle, ResourceType } from '../api';
import { ResourceBase_GL } from './ResourceBase';
export declare class RenderBundle_GL extends ResourceBase_GL implements RenderBundle {
    type: ResourceType.RenderBundle;
    private commands;
    push(f: () => void): void;
    replay(): void;
}
