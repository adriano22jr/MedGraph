import type { DisplayObject, ParsedImageStyleProps } from '@antv/g-lite';
import type { ImagePool } from '@antv/g-plugin-image-loader';
import type { StyleRenderer } from './interfaces';
export declare class ImageRenderer implements StyleRenderer {
    private imagePool;
    constructor(imagePool: ImagePool);
    render(context: CanvasRenderingContext2D, parsedStyle: ParsedImageStyleProps, object: DisplayObject): void;
}
