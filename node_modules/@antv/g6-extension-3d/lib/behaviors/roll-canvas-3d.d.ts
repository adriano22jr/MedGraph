import type { BaseBehaviorOptions, RuntimeContext, ShortcutKey } from '@antv/g6';
import { BasePlugin } from '@antv/g6';
export interface RollCanvas3DOptions extends BaseBehaviorOptions {
    enable?: boolean;
    /**
     * <zh/> 按下该快捷键配合滚轮操作进行旋转
     *
     * <en/> Press this shortcut key to rotate with the mouse wheel
     */
    trigger?: ShortcutKey;
    /**
     * <zh/> 灵敏度
     *
     * <en/> Sensitivity
     */
    sensitivity?: number;
}
export declare class RollCanvas3D extends BasePlugin<RollCanvas3DOptions> {
    static defaultOptions: Partial<RollCanvas3DOptions>;
    private shortcut;
    private get camera();
    constructor(context: RuntimeContext, options: RollCanvas3DOptions);
    private getAngle;
    private onRoll;
    private bindEvents;
}
