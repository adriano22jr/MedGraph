"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ZoomCanvas3D = void 0;
const g6_1 = require("@antv/g6");
const util_1 = require("@antv/util");
class ZoomCanvas3D extends g6_1.ZoomCanvas {
    zoom = async (value, event, animation) => {
        if (!this.validate(event))
            return;
        const { graph } = this.context;
        const { sensitivity, onFinish } = this.options;
        const ratio = 1 + ((0, util_1.clamp)(value, -50, 50) * sensitivity) / 100;
        const zoom = graph.getZoom();
        this.context.canvas.getCamera().setZoom(zoom * ratio);
        onFinish?.();
    };
}
exports.ZoomCanvas3D = ZoomCanvas3D;
//# sourceMappingURL=zoom-canvas-3d.js.map