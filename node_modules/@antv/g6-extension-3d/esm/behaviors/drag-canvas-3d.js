import { DragCanvas } from '@antv/g6';
/**
 * <zh/> 平移画布
 *
 * <en/> Pan canvas
 */
export class DragCanvas3D extends DragCanvas {
    async translate(offset, animation) {
        this.context.canvas.getCamera().pan(-offset[0], -offset[1]);
    }
}
//# sourceMappingURL=drag-canvas-3d.js.map