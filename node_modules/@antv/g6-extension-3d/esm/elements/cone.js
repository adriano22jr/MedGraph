import { ConeGeometry } from '@antv/g-plugin-3d';
import { deepMix } from '@antv/util';
import { createGeometry } from '../utils/geometry';
import { BaseNode3D } from './base-node-3d';
export class Cone extends BaseNode3D {
    static defaultStyleProps = {
        // baseRadius, peakRadius, height
        size: [24, 0, 48],
        heightSegments: 5,
        capSegments: 20,
    };
    constructor(options) {
        super(deepMix({}, { style: Cone.defaultStyleProps }, options));
    }
    getGeometry(attributes) {
        const size = this.getSize();
        const { baseRadius = size[0] / 2, peakRadius = size[1] / 2, height = size[2], heightSegments, capSegments, } = attributes;
        return createGeometry('cone', this.device, ConeGeometry, {
            baseRadius,
            peakRadius,
            height,
            heightSegments,
            capSegments,
        });
    }
}
//# sourceMappingURL=cone.js.map